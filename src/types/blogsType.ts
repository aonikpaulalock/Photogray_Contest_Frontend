export type TBlog = {
  id: number;
  image: string; // Assuming the images are strings (URLs or paths), adjust if they are imports.
  date: string;
  views: string;
  comments: string;
  authorName: string;
  authorRole: string;
  description: string;
};