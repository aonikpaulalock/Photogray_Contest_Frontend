import { Outlet } from "react-router-dom";

const ContestHolderDashboard = () => {
  return (
    <div>
    <h1>Dashboard - Contest Holder</h1>
    <Outlet />
  </div>
  )
};

export default ContestHolderDashboard;