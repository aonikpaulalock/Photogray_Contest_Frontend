const tags = [
  "Nature",
  "Hills",
  "Model",
  "Product",
  "3d",
  "Fashion",
];

// Mapping tags to the format required for the select options
export const tagOptions = tags.map((tag) => ({
  value: tag,
  label: tag,
}));